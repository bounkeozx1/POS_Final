# วิธีรันระบบ POS ให้เชื่อมฐานข้อมูล MySQL จริง (XAMPP)

ระบบนี้มี **โหมดเดียว: MySQL** ไม่มีโหมดสำรองที่เก็บข้อมูลในเบราว์เซอร์อีกแล้ว

| เปิดผ่าน | ผลลัพธ์ |
|----------|---------|
| **XAMPP / Apache + PHP** (`http://localhost/POS-SST-main/`) | อ่าน/เขียน **MySQL** จริง ถาวร ✅ |
| เปิดไฟล์ตรง ๆ (`file://`) / static server อื่น / GitHub Pages | **ขึ้นหน้า error** พร้อมวิธีแก้ ❌ |

> ตั้งใจให้ "พังให้เห็น" — เวอร์ชันก่อนหน้าจะเงียบ ๆ สลับไปเก็บข้อมูลใน
> localStorage ของเบราว์เซอร์แทน ทำให้เข้าใจผิดว่าบันทึกลงฐานข้อมูลแล้ว
> ทั้งที่ MySQL ว่างเปล่า

---

## ขั้นตอนติดตั้ง (ครั้งเดียว)

### 1) ติดตั้ง XAMPP
ดาวน์โหลดจาก https://www.apachefriends.org แล้วติดตั้ง (จะได้ Apache + MySQL + PHP + phpMyAdmin ในชุดเดียว)

### 2) วางโปรเจกต์ไว้ใน htdocs
คัดลอกโฟลเดอร์ทั้งหมด `POS-SST-main` ไปไว้ที่:
```
C:\xampp\htdocs\POS-SST-main
```

### 3) เปิดบริการ
เปิด **XAMPP Control Panel** → กด **Start** ทั้ง **Apache** และ **MySQL**

### 4) สร้างฐานข้อมูล (import โครงสร้าง)
1. เปิดเบราว์เซอร์ไปที่ http://localhost/phpmyadmin
2. คลิกแท็บ **Import** (นำเข้า)
3. เลือกไฟล์ `database.sql` ในโปรเจกต์ → กด **Go/ลงมือ**
4. จะได้ฐานข้อมูล **`pos_ground_camp`** พร้อมตารางทั้งหมด (`tbl_user`, `tbl_product`, `tbl_sale`, ...)
   **และข้อมูลตั้งต้นครบแล้ว** — เมนู 14 อย่าง, 5 ประเภท, 6 โต๊ะ, 2 ผู้ใช้, 5 วัตถุดิบ, ตั้งค่าร้าน

> ตารางธุรกรรม (`tbl_sale`, `tbl_purchase`, `tbl_import`, `tbl_stock_log`) **ตั้งใจให้ว่าง**
> ข้อมูลจะเกิดจากการใช้งานจริงเท่านั้น ไม่มีบิลปลอม/ข้อมูลสุ่มใส่ไว้ให้

### 5) เปิดระบบ
เปิดเบราว์เซอร์ไปที่:
- หน้าลูกค้า: **http://localhost/POS-SST-main/**
- หน้าแอดมิน: **http://localhost/POS-SST-main/admin.html** (ล็อกอิน `admin` / `1234`)

ข้อมูลทุกอย่างที่เห็นบนหน้าจอมาจาก MySQL ทั้งหมด ไม่มีข้อมูลฝังในไฟล์ JavaScript

---

## พิสูจน์ว่าเชื่อม DB จริง (ทดสอบตอนสอบ)

**ทิศทาง 1 — แก้ในแอป → เข้า DB จริง**
1. เข้า `admin.html` → เพิ่ม/แก้/ลบ เมนู
2. เปิด phpMyAdmin → ตาราง `tbl_product` → กด Browse → **เห็นการเปลี่ยนแปลงทันที**

**ทิศทาง 2 — แก้ใน DB → ขึ้นในแอป**
1. ใน phpMyAdmin แก้ราคาสินค้าใน `tbl_product` (กด Edit ที่แถวนั้น)
2. กลับไปที่หน้าแอป → **กด Refresh (F5)** → เห็นราคาใหม่

> ต่างจากของเดิมที่แก้แล้วไม่เข้า DB (เก็บแค่ในเบราว์เซอร์) — ตอนนี้ทุกอย่างอยู่ใน MySQL จริง

**ทิศทาง 3 — ใช้ 2 หน้าจอพร้อมกัน (เหมือนหน้าร้านจริง)**
1. เปิด `index.html` (ลูกค้า) และ `admin.html` (แอดมิน) — จะคนละแท็บ **หรือคนละเครื่อง** ก็ได้
2. ลูกค้าสั่งอาหาร → แอดมินหน้า "ครัว" เห็นออเดอร์ใหม่เองภายใน ~5 วินาที
3. แอดมินกดรับออเดอร์ → เช็ค `tbl_sale` ใน phpMyAdmin → ออเดอร์ของลูกค้า **ยังอยู่ครบ**

> การซิงก์ทำโดยหน้าเว็บถาม `api/index.php` ทุก 5 วินาที (ดู `startPolling()` ใน `Db.js`)
> ของเดิมใช้ event `storage` ของ localStorage ซึ่งทำงานเฉพาะแท็บในเบราว์เซอร์เดียวกัน
> เครื่องเดียวกัน — มือถือลูกค้ากับคอมแอดมินจึงมองไม่เห็นกัน

### เขียนรายงาน: ใช้ SQL ดึงข้อมูลได้จริง
เพราะคอลัมน์วันที่เป็น `DATETIME` (ไม่ใช่ข้อความ) จึงเขียนรายงานด้วย SQL ตรง ๆ ได้:

```sql
SELECT DATE(created_at) AS sale_date, COUNT(*) AS bills, SUM(total_amount) AS revenue
FROM tbl_sale WHERE status = 'done' GROUP BY DATE(created_at) ORDER BY 1 DESC;
```

และตารางเชื่อมกันด้วย **FOREIGN KEY** จริง (12 ความสัมพันธ์) เช่น:

```sql
SELECT p.name_lo AS menu, c.name_lo AS category
FROM tbl_product p JOIN tbl_category c ON c.cate_id = p.cate_id;
```

---

## ส่งโปรเจกต์ให้คนอื่น (ให้เขาเห็นเมนู/รูป ชุดล่าสุด)

**อ่านตรงนี้ก่อนส่งทุกครั้ง** — เมนูกับรูปที่แก้ผ่านหน้า admin **ไม่ได้อยู่ในโค้ด**
มันอยู่คนละที่กัน 2 แห่ง และไม่มีที่ไหนขึ้น git เอง:

| ของ | เก็บอยู่ที่ | ขึ้น git เองไหม |
|---|---|---|
| ชื่อ / ราคา / เส้นทางรูป | ตาราง MySQL | ❌ |
| ไฟล์รูปที่อัปโหลด | `images/menu/` ของ **ชุดที่ Apache เปิด** | ❌ |
| `database.sql` | ไฟล์ในโปรเจกต์ | ✅ แต่เป็นข้อมูล **ชุดแรก** |

ถ้าส่งไปเฉย ๆ คนรับจะ import `database.sql` ชุดแรก → **เห็นเมนูและรูปชุดเดิมเสมอ**
ต่อให้เราแก้ไปกี่รอบก็ตาม

**วิธีที่ถูก — รันคำสั่งเดียวก่อน commit:**

```bash
php tools/export-db.php
```

สคริปต์จะ (1) เขียนส่วนข้อมูลใน `database.sql` ใหม่จาก MySQL ที่ใช้อยู่จริง โดย
**ไม่แตะโครงสร้างตารางข้างบน** และ (2) คัดลอกไฟล์รูปที่เมนูอ้างถึงเข้ามาในโปรเจกต์ให้

ถ้าโปรเจกต์แยกเป็น 2 ชุด (ชุด git กับชุดใน `htdocs`) สคริปต์จะไปดึงรูปจาก
`C:\xampp\htdocs\<ชื่อโฟลเดอร์เดียวกัน>` ให้เอง ถ้าวางไว้ที่อื่นให้ระบุเอง:

```bash
php tools/export-db.php --from="D:\path\to\ชุดที่ Apache เปิด"
```

จากนั้นค่อย `git add -A && git commit && git push`

> **บิลขาย / สั่งซื้อ / นำเข้า / log สต็อก ไม่ถูก export ไปด้วยโดยตั้งใจ**
> คนที่รับไปควรเริ่มจากศูนย์ และ `order_num` จะถูกรีเซ็ตเป็น 1001, โต๊ะกลับเป็นว่าง

---

## ไฟล์ที่เกี่ยวกับฐานข้อมูล

| ไฟล์ | หน้าที่ |
|------|---------|
| `database.sql` | โครงสร้างตาราง **+ ข้อมูลตั้งต้น** (import ใน phpMyAdmin) — สร้างใหม่ได้ด้วย `tools/export-db.php` |
| `tools/export-db.php` | เขียน `database.sql` ใหม่จากข้อมูลจริง + ดึงรูปที่อัปโหลดเข้ามาใน repo |
| `api/config.php` | ตั้งค่าเชื่อมต่อ MySQL (host/พอร์ต/user/pass) — หาพอร์ตให้เองจาก `DB_PORTS` |
| `api/config.local.php` | *(ไม่บังคับ, ไม่ขึ้น git)* ทับค่าเฉพาะเครื่องนั้น เช่นพอร์ต/รหัสผ่าน |
| `api/index.php` | PHP API ที่ทำ เพิ่ม/แก้/ลบ/ดึง ข้อมูลจริงกับ MySQL |
| `Db.js` | ฝั่งเว็บ — เรียก API ข้างบนอย่างเดียว ไม่มี fallback |
| `.htaccess` | สั่งห้ามเบราว์เซอร์ cache ไฟล์ `.html`/`.js` |

> **ทำไมต้องมี `.htaccess`:** ถ้าเบราว์เซอร์จำหน้าเก่าไว้ มันจะโหลด JavaScript
> เวอร์ชันเก่ามาใช้ ซึ่งอาจเขียนข้อมูลผิดลงฐานข้อมูลโดยที่เราไม่รู้ตัว
> เจอมาแล้วตอนแก้ระบบนี้ — หาสาเหตุยากมาก

### เรื่องรูปภาพเมนู (ช่อง `tbl_product.img`)
รูปที่อัปโหลดผ่านหน้า admin จะถูกย่อเหลือกว้าง 600px แล้วเก็บเป็น **data URL (base64)**
ลงฐานข้อมูลโดยตรง คอลัมน์เป็น `MEDIUMTEXT` จึงเก็บได้เต็ม (ปกติ 5,000–10,000 ตัวอักษร)

เมนูตั้งต้นใน `database.sql` เก็บเป็น **เส้นทางไฟล์** (`images/menu/xx.jpg`) ไม่ใช่ base64
ยกเว้น **ເບຍລາວ** กับ **ນ້ຳດື່ມ** ที่ยังไม่มีไฟล์รูปในโปรเจกต์ → ใส่ `NULL` ไว้
หน้าจอจะแสดง emoji (🍺 / 💧) แทน ไม่ใช่รูปแตก
ถ้าจะใส่รูปจริง: วางไฟล์ใน `images/menu/` แล้วอัปโหลดผ่านหน้า admin หรือแก้ `img` ใน DB

พิสูจน์ว่ารูปอยู่ใน DB จริง ไม่ใช่แค่ในเบราว์เซอร์:
1. หน้า admin → แก้เมนู → เลือกรูปใหม่ → บันทึก
2. เปิดหน้าเว็บในเบราว์เซอร์เครื่องอื่น (หรือโหมดไม่ระบุตัวตน)
3. รูปยังขึ้นเหมือนเดิม เพราะโหลดมาจาก MySQL ล้วน ๆ

```sql
SELECT prod_id, name_lo, CHAR_LENGTH(img) AS img_size FROM tbl_product;
```

> ถ้า `img_size` เป็น 255 พอดีทุกแถว แปลว่าโดนตัดข้อมูล — ต้อง import `database.sql`
> ฉบับล่าสุด (เวอร์ชันเก่าใช้ `VARCHAR(255)` ทำให้รูปเสียแบบเงียบ ๆ)

### เรื่องรหัสผ่านผู้ใช้ (ช่อง `tbl_user.password`)
เก็บเป็น **SHA-256 + salt เฉพาะคน + วนซ้ำ 1,000 รอบ** รูปแบบ `s1$<salt>$<hash>`
เปิด phpMyAdmin ดูได้เลยว่าไม่มีรหัสผ่านตัวจริงอยู่ในฐานข้อมูล และ `admin`
กับ `cashier` ที่ใช้รหัส `1234` เหมือนกันจะได้ค่า hash **ต่างกันคนละอัน** (เพราะ salt)

> ข้อจำกัดที่ควรเขียนในรายงานตามจริง: ระบบตรวจรหัสผ่านฝั่งเบราว์เซอร์
> การแฮชจึงกัน "คนเปิดตารางแล้วเห็นรหัสผ่าน" ได้ แต่ยังไม่ใช่ระบบยืนยันตัวตน
> ระดับใช้งานจริง ซึ่งต้องตรวจฝั่งเซิร์ฟเวอร์

### ถ้า MySQL ตั้งรหัสผ่านไว้
แก้ `api/config.php`:
```php
define('DB_USER', 'root');
define('DB_PASS', 'รหัสผ่านของคุณ');
```
(ค่าเริ่มต้นของ XAMPP คือ user `root` ไม่มีรหัสผ่าน)

---

## ปัญหาที่พบบ่อย

- **หน้าเว็บว่าง / เมนูไม่ขึ้น** → เปิด DevTools (F12) แท็บ Console/Network ดู error ของ `api/index.php`
- **มี toast แดง "ບັນທຶກລົງຖານຂໍ້ມູນບໍ່ສຳເລັດ"** → เขียนลง MySQL ไม่ผ่าน (เช่น ตั้งชื่อผู้ใช้ซ้ำ)
  ระบบจะดึงข้อมูลจริงจาก DB กลับมาแสดงให้เอง — แก้ตามข้อความแล้วบันทึกใหม่
  (เมื่อก่อนกรณีนี้จะ **เงียบ** ทำให้เห็นข้อมูลบนจอทั้งที่ DB ไม่มีจริง)
- **`api/index.php` โชว์เป็นโค้ด PHP** → แปลว่าเปิดผ่านไฟล์ตรง ๆ ไม่ใช่ผ่าน Apache — ต้องเข้า `http://localhost/...` เท่านั้น
- **เชื่อม DB ไม่ได้ / ขึ้นหน้า 🔌** → ตรวจ 3 อย่าง:
  1. XAMPP Start ทั้ง **Apache** และ **MySQL** แล้วหรือยัง
  2. import `database.sql` แล้วได้ฐาน `pos_ground_camp` จริงไหม
  3. **อยากรู้สาเหตุจริง ๆ ให้เปิด `http://localhost/POS-SST-main/api/index.php` ตรง ๆ**
     มันจะบอกเป็นข้อความว่าลองพอร์ตไหนไปบ้าง และแต่ละพอร์ตล้มเพราะอะไร
     (ต่อไม่ได้ / รหัสผ่านผิด / ไม่มีฐาน `pos_ground_camp`)

> **เรื่องพอร์ต — ปกติไม่ต้องแก้อะไร**
> `api/config.php` ใช้ `DB_PORTS` เป็น *รายการ* (ค่าเริ่มต้น `'3307,3306'`) แล้วลองไล่จากซ้ายไปขวา
> ตัวไหนต่อติดก่อนใช้ตัวนั้น — XAMPP มาตรฐานอยู่ 3306 ส่วนเครื่องที่ย้ายหนี `MySQL97` อยู่ 3307
> ทั้งสองแบบจึงใช้ไฟล์เดียวกันได้เลย
>
> การทดลองต่อแต่ละครั้งใส่ชื่อฐาน + user/pass ไปด้วย ถ้าพอร์ตนั้นเป็น MySQL คนละตัว
> มันจะล้มแล้วข้ามไป **ไม่ใช่หยิบฐานผิดมาใช้**
>
> ถ้าเครื่องไหนพอร์ตหรือรหัสผ่านต่างออกไป ให้สร้างไฟล์ `api/config.local.php`
> (ไฟล์นี้ถูก gitignore ไว้ ไม่ถูก push ขึ้น GitHub) แล้วใส่เฉพาะค่าที่อยากทับ:
> ```php
> <?php
> define('DB_PORTS', '3306');
> define('DB_PASS', 'รหัสผ่านของเครื่องนั้น');
> ```
- **แก้ใน phpMyAdmin แล้วหน้าเว็บไม่เปลี่ยน** → กด F5, และ **ปิดแท็บแอปเก่าที่เปิดค้างไว้ก่อน**
  แท็บเก่าถือข้อมูลชุดเดิมอยู่ในหน่วยความจำ พอมันบันทึกอะไรจะส่งข้อมูลเก่าทับของที่เพิ่งแก้
- **อยากล้างข้อมูลเริ่มใหม่** →
  - ปุ่ม reset ในหน้า admin = ล้างเฉพาะ **ข้อมูลธุรกรรม** (บิล, สั่งซื้อ, นำเข้า, log สต็อก) เมนู/ผู้ใช้ยังอยู่
  - อยากล้างทั้งหมดกลับเป็นค่าโรงงาน = import `database.sql` ซ้ำใน phpMyAdmin
