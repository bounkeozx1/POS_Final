# OS
Thumbs.db
Desktop.ini
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Logs / temp
*.log
*.tmp
*.bak

# Uploaded files at runtime (keep the seeded menu images)
images/uploads/

# Per-machine DB overrides (port / password) — must never be shared
api/config.local.php
